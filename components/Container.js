/**
 * This container can be used to ensure that all pages and components use
 * equal padding and margins.
 */

export default function Container({ children }) {
  return (
    <div className="">
      {children}
    </div>
  )
}
